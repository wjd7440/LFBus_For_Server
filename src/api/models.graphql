type User {
  id: ID!
  name: String!
}

type BusStation {
  id: ID!
  name: String!
  busStationNo: Int!
}

type BusInfo {
  id: ID!
  BUS_TYPE: Int!
  CAR_REG_NO: String!
  CHARACTER: String!
  COMP_CD: Int!
}
