type BusStations {
  busStations: [BusStations!]!
  count: Int
}

type Query {
  KioskBusStationList(
    keyword: String
    orderBy: String
    skip: Int
    after: String
    before: String
    first: Int
    last: Int
  ): BusStations!
}
