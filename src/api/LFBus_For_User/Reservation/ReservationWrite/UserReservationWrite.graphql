type Mutation {
  UserReservationWrite(
    CAR_REG_NO: String!
    userId: String!
    BUS_NODE_ID: Int!
    departureStation: String!
    arrivalStation: String!
    equipment: String
    memo: String
  ): Boolean!
}
