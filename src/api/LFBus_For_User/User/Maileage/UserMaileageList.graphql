type Maileages {
  maileages: [Maileage!]!
  count: Int
}

type Query {
  UserMaileageList(
    keyword: String
    orderBy: String
    skip: Int
    after: String
    before: String
    first: Int
    last: Int
  ): Maileages!
}
