type Maileages {
  maileages: [Maileage!]!
  count: Int
}

type Query {
  UserMailegeList(
    keyword: String
    orderBy: String
    skip: Int
    after: String
    before: String
    first: Int
    last: Int
  ): Maileages!
}
