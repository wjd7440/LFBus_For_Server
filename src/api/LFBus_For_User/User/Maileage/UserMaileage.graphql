type Maileages {
  maileages: [Maileage!]!
  count: Int
}

type Query {
  UserMailegeList(
    userId: String!
    keyword: String
    orderBy: String
    skip: Int
    after: String
    before: String
    first: Int
    last: Int
  ): Maileages!
}
