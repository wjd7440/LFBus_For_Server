type BusRotations {
  busRotations: [BusRotation!]!
  count: Int
}

type Query {
  UserBusRotationList(
    ROUTE_CD: String
    keyword: String
    orderBy: String
    skip: Int
    after: String
    before: String
    first: Int
    last: Int
  ): BusRotations!
}
