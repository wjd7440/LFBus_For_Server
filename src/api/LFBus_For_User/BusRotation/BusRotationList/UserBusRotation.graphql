type BusRotations {
  busRotations: [BusRotation!]!
  count: Int
}

type Query {
  UserBusRotationList(
    ROUTE_TP: Int
    keyword: String
    orderBy: String
    skip: Int
    after: String
    before: String
    first: Int
    last: Int
  ): BusRotations!
}
