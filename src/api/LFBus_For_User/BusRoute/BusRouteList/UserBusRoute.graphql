type BusRoutes {
  busRoutes: [BusRoute!]!
  count: Int
}

type Query {
  UserBusRouteList(
    ROUTE_TP: Int
    keyword: String
    orderBy: String
    skip: Int
    after: String
    before: String
    first: Int
    last: Int
  ): BusRoutes!
}
