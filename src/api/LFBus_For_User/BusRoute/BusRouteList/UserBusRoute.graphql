type BusRoutes {
  busRoutes: [BusRoute!]!
  count: Int
}

type Query {
  UserBusRouteList(
    keyword: String
    orderBy: String
    skip: Int
    after: String
    before: String
    first: Int
    last: Int
  ): BusRoutes!
}
