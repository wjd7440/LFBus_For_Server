type User {
  id: ID! @id
  name: String!
}

type BusStation {
  id: ID! @id
  name: String!
  busStationNo: Int!
}

type BusInfo{
  BUS_TYPE: Int!
  CAR_REG_NO: String!
  CHARACTER: Int!
  COMP_CD: Int!
}