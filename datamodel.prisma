type User {
  id: ID! @id
  name: String!
}

type BusStation {
  id: ID! @id
  name: String!
  busStationNo: Int!
}

type BusInfo{
  CAR_REG_NO: String!
  BUS_TYPE: Int!  
  CHARACTER: Int!
  COMP_CD: Int!
}